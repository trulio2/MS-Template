# Node.js Service

Minimal Node.js HTTP service with a `GET /` endpoint.

```bash
npm install
npm start
```

The service listens on `http://localhost:3001` by default. Set `PORT` to use a different port.

```json
{ "message": "Hello from the Node.js service!" }
```
