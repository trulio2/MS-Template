# Go Service

Minimal Go HTTP service with a `GET /` endpoint.

```bash
go run .
```

The service listens on `http://localhost:8080` by default. Set `PORT` to use a different port.

```json
{ "message": "Hello from the Go service!" }
```
